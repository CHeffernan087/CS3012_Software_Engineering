from graph_lowestCommonAncestor import Node
class BST:

	def put(key, value):
		if(self.root== None):
			self.root = Node(key,value)

		else:
			put(self.root,key,value)

	def put(n, key, val):
		print("test")

my_list = BST();
my_list.put(9,"Conor") 